<% if @lost_pet.errors.any? %>
  <%= @lost_pet.errors.count %>errors prohibited this obj from being saved:
  <ul>
    <% @lost_pet.errors.full_messages.each do |message| %>
      <li><%= message %></li>
    <% end %>
  </ul>
<% end %>

<h1>迷子掲示板投稿</h1>
<%= form_with model: @lost_pet do |f| %>
<h4>タイトル</h4>
  <%= f.text_field :title %>

  <h4>本文</h4>
  <%= f.text_area :body %>

  <h4>画像</h4>
  <%= f.file_field :lost_pet_image, accept: "image/*" %>

  <%= f.submit '投稿' %>
<% end %>