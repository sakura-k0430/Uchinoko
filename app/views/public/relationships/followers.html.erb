<h3>
  <i class="fa-solid fa-fish-fins" style="color: #20b2aa;"></i>
  フォロワー一覧
  <i class="fa-solid fa-fish-fins" style="color: #20b2aa;"></i>
</h3>

<button type="button" class="btn btn-outline-primary">
  <i class="fa-solid fa-user-plus"></i><%=link_to "グループを作成する",new_group_path %>
</button>
<button type="button" class="btn btn-outline-secondary">
<i class="fa-solid fa-people-group"></i><%=link_to "グループ一覧",groups_path %>
</button>

<table class="table table-hover table-inverse">
  <tbody>
    <tr>
      <th>ユーザーアイコン</th>
      <th>ユーザー名</th>
      <th>フォロー数</th>
      <th>フォロワー数</th>
     </tr>
  </tbody>
  <tbody>
  <% if @customers.exists? %>
  <% @customers.each do |customer| %>
      <tbody>
        <tr>
          <td><%= image_tag customer.get_profile_image(80,80), class:"mt-3 rounded-circle"; %></td>
          <td><%= link_to customer.name, customer_path(customer.id) %></td>
          <td><%= customer.followings.count %></td>
          <td><%= customer.followers.count %></td>
        </tr>
      </tbody>
  <% end %>
<% else %>
  <p style="color: #20b2aa;">ユーザーはいません</p>
<% end %>
</table>