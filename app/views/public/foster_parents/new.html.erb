<% if @foster_parent.errors.any? %>
  <%= @foster_parent.errors.count %>errors prohibited this obj from being saved:
  <ul>
    <% @foster_parent.errors.full_messages.each do |message| %>
      <li><%= message %></li>
    <% end %>
  </ul>
<% end %>

<h1>里親掲示板投稿</h1>
<%= form_with model: @foster_parent do |f| %>
<h4>タイトル</h4>
  <%= f.text_field :title %>

  <h4>本文</h4>
  <%= f.text_area :body %>

  <h4>画像</h4>
  <%= f.file_field :foster_parent_image, accept: "image/*" %>

  <%= f.submit '投稿' %>
<% end %>